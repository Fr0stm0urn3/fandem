const MiniTitle = ({ title }) => {
  return (
    <div className="uppercase text-[#868686] font-[400] text-[14px] border border-[#444] py-[14px] px-4 rounded-[33px] inline-flex mb-2 lg:mb-3 z-10">
      {title}
    </div>
  )
}

export default MiniTitle
